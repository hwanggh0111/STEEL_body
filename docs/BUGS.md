# 버그 보고서 — 파칭코 / 사다리 / 미니게임

작성일: 2026-08-14
최종 수정: 2026-08-17
대상: `frontend/src` — 파칭코, 사다리, 원판 피하기, 티켓·레벨 회계
검증 방법: 코드 정독 + 산술 검증. **런타임으로 재현한 것은 아님** (각 항목에 재현 절차 기재)

우선순위는 **피해 크기 × 발생 가능성**으로 매겼습니다.

## 현황 (2026-08-17)

| | 항목 | 상태 |
|---|---|---|
| P0-1 | 연출 중 이탈 시 티켓 환불 | ✅ 수정 |
| P0-2 | 구매 티켓 localStorage 조작 | ✅ 방침 확정 (받아들이고 문서화) |
| P1-1 | 상한 초과 티켓 소멸 | ✅ 수정 (소멸 안내 + 상한 표시) |
| P1-2 | 다회 뽑기 합계 2^53 초과 | ✅ 수정 |
| P2-1 | 사다리 타이머 배열 누적 | ✅ 수정 |
| P2-2 | 홈 파칭코 EXP 스냅샷 | ✅ 수정 |
| P2-3 | 만렙 근처 배너 이전 레벨 오표시 | ✅ 수정 |
| P3 | localStorage 쓰기 실패 미처리 | ✅ 수정 |

P0-2와 P1-1은 **어떻게 고칠지가 아니라 어떤 게임을 만들지의 문제**였고, 2026-08-17에 결정했습니다.

---

## P0-1. 연출 도중 이탈하면 티켓이 환불된다 (사다리 3.6초 창) — ✅ 수정됨

**파일** `frontend/src/components/LadderGame.jsx:117` (언마운트 `release`), `:169` (`play` 호출)

**무엇이 잘못됐나**

티켓 회계가 2단계입니다.

1. 판을 시작할 때 `reserve(n)` — `pending`만 올라감 (localStorage 미기록)
2. 연출이 끝날 때 `play()` — 그제서야 `used`가 올라가고 localStorage에 기록

그 사이에 컴포넌트가 언마운트되면 cleanup이 `release(inFlightRef.current)`를 불러 **예약을 되돌립니다.** `used`는 끝내 오르지 않습니다. 새로고침해도 같습니다 — `pending`은 저장하지 않으므로 사라지고, `used`는 애초에 안 올랐습니다.

**왜 치명적인가**

사다리는 **시작 즉시 5칸의 보상이 전부 화면에 표시되고, 가로줄도 전부 보입니다.** 즉 눈으로 경로를 따라가면 3.6초 연출이 끝나기 훨씬 전에 결과를 알 수 있습니다. 꽝이면 탭을 벗어나거나 새로고침 → **티켓 100장 전액 환불.**

기대값을 무한대로 만드는 구조적 결함입니다. 꽝만 골라서 무를 수 있으면 사다리는 손해 볼 수 없는 게임이 됩니다.

**재현**

1. `/pachinko` 이동, 티켓 100장 이상 확보
2. 사다리 시작점 클릭
3. 선이 도착 칸에 닿기 전에 보상 위치를 눈으로 확인
4. 꽝이면 즉시 새로고침
5. 보유 티켓이 줄지 않았는지 확인

**파칭코도 같은 구조지만 창이 훨씬 좁습니다** — 마지막 릴이 2008ms에 멈추고 `play()`는 2200ms에 커밋되므로 **약 190ms**. 실현 가능성은 낮지만 원인은 동일합니다.

**어떻게 고쳤나** (2026-08-17)

`used`를 **판 시작 시점에 확정**하고, 연출은 이미 정해진 결과를 보여주기만 하게 바꿨습니다. 원판 피하기의 `plateStore.startRun()`과 같은 방식입니다.

스토어 API가 두 단계로 갈렸습니다.

- `beginPlay(cost)` — 판을 시작하는 즉시 `used` 증가 + localStorage 기록
- `award(prize, mode)` / `awardMany(rows, mode)` — 연출이 끝나면 보상만 반영

`reserve` / `release` / `pending`은 필요 없어져 전부 걷어냈습니다 (`PachinkoPage`, `MiniGamePage`, `dev/setLevel.js`의 참조 포함).

**대신 생기는 문제도 같이 막았습니다.** 티켓이 먼저 나가므로 연출 도중 이탈하면 티켓만 날리게 됩니다. 그래서 뽑은 보상을 `inFlightRef`에 들고 있다가 **언마운트 때 정산**합니다 — 티켓이 나갔으면 보상은 반드시 지급됩니다. 새로고침은 원래 `used`가 이미 기록돼 있으므로 티켓만 정상 소모되고, 그 판의 보상은 받지 못합니다(결과를 못 본 채 나간 것이므로 의도된 동작).

---

## P0-2. 구매 티켓이 localStorage에 있어 조작 가능 — ✅ 방침 확정 (b)

**파일** `frontend/src/data/plateData.js:112` (`PLS.purchased`), `frontend/src/components/PachinkoSystem.jsx:76` (`earnedTickets`)

**무엇이 잘못됐나**

원래 티켓 설계는 이렇습니다 (`pachinkoData.js:4` 주석):

> 티켓은 "발급량 = 기록 수에서 계산" + "사용량만 localStorage 누적" 구조.
> **발급량을 저장하지 않으므로 티켓 자체를 조작할 수 없습니다.**

원판 피하기로 산 티켓(`purchased`)을 발급량에 더하면서 이 보장이 깨졌습니다. `purchased`는 저장되는 값이라 콘솔 한 줄로 무제한 발급됩니다.

```js
localStorage.setItem('steelbody_plate_tickets', '999999')
```

하루 판 수 제한(`dailyPlays`)으로 벌이를 묶어놨지만, 그건 정직하게 플레이할 때만 유효합니다.

**참고** `used`도 localStorage라 `0`으로 되돌리면 티켓이 재충전됩니다. 이건 원래부터 있던 구멍이지만, 같은 층위의 문제라 같이 판단해야 합니다.

**선택지**

- (a) 서버에 티켓 원장을 두고 검증 — 정석이지만 백엔드 작업 필요
- (b) 조작 가능성을 받아들이고 문서화 — 혼자 쓰는 앱이면 합리적
- (c) 서명/체크섬으로 캐주얼한 조작만 차단 — 비용 대비 효과는 낮음

**결정: (b)** (2026-08-17)

레벨은 남과 겨루는 값이 아니라 자기 기록을 보는 지표라, 서버 원장을 둘 만큼의 값어치가 없다고 판단했습니다. 조작하고 싶은 사람은 자기 숫자만 망칠 뿐입니다.

**대신 코드가 거짓말하지 않게 맞췄습니다.** 원래 `pachinkoData.js` 상단에 "발급량을 저장하지 않으므로 티켓 자체를 조작할 수 없습니다"라고 적혀 있었는데, `purchased`가 생기면서 사실이 아니게 된 채 남아 있었습니다. 이제 어떤 키가 열려 있는지까지 적어 뒀습니다.

```
steelbody_plate_tickets   원판 피하기로 산 티켓
steelbody_pachinko_used   사용량 (0으로 되돌리면 티켓이 재충전됨)
steelbody_pachinko_exp    누적 EXP
```

`PachinkoSystem.jsx`의 `earnedTickets` 주석에 있던 "(조작 불가)"도 같이 지웠습니다.

**이 결정이 뒤집히는 조건** — 랭킹처럼 **남과 비교하는 요소**가 들어오는 순간 (b)는 성립하지 않습니다. 그때는 (a)가 필요합니다.

---

## P1-1. 상한 초과 티켓 소멸이 유저 자산을 태운다 — ✅ 수정됨

**파일** `frontend/src/pages/PachinkoPage.jsx:38` (`trimOverflow` effect), `frontend/src/store/pachinkoStore.js:38`

**무엇이 잘못됐나**

`available = min(earned - used, maxStack)`이 표시만 상한에 묶어서, `earned - used > maxStack`인 계정은 티켓을 써도 숫자가 줄지 않는 **무한 티켓 버그**가 있었습니다. 이건 `trimOverflow`로 고쳤습니다 (넘친 만큼 `used`로 밀어 실제 소멸).

**남은 문제:** 이제 상한 초과분이 **진짜로 사라집니다.** 운영 `maxStack`은 150이고 `earned`는 운동 3회당 1장이므로, **운동 450회를 기록한 유저가 파칭코 페이지를 열기만 해도** 그때까지 안 쓴 티켓이 150장으로 잘립니다. 예고도 없고 되돌릴 수도 없습니다.

의도된 설계(무한 적립 방지)를 실제로 작동시킨 것이라 "버그"라 부르긴 애매하지만, **유저가 체감하는 손실**이라 그대로 두면 민원이 됩니다.

**선택지**

- 소멸 전에 안내 (모달 또는 토스트 1회)
- 또는 상한을 크게 올려 실질적으로 안 걸리게
- 또는 상한을 없애고 `available = earned - used`로 단순화

**결정: 상한은 유지하고 안내를 붙인다** (2026-08-17)

무한 적립 방지라는 원래 의도는 살리되, 말없이 자산이 타는 것만 막았습니다. 두 군데에서 알립니다.

| 시점 | 어디 | 내용 |
|---|---|---|
| **소멸 전** | 파칭코 헤더 티켓 숫자 아래 | `상한 · 더 안 쌓여요` (상한에 닿아 있는 동안 계속) |
| 소멸 직후 | 토스트 (warning) | `보유 상한 150장을 넘은 티켓 N장이 사라졌어요` |

`trimOverflow`는 원래부터 소멸량을 반환하고 있었는데 호출부가 버리고 있었습니다. 그 값을 그대로 씁니다.

미니게임 쪽은 이미 막혀 있습니다 — `PlateDodge`가 `ticketRoom`으로 상한 초과 구매를 차단하고 `ticketFull` 상태를 표시합니다.

---

## P1-2. 다회 뽑기 합계가 2^53을 넘으면 표시가 부정확 — ✅ 수정됨

**파일** `frontend/src/store/pachinkoStore.js:85` (`sum`), `frontend/src/components/PachinkoSystem.jsx` 요약 패널

**무엇이 잘못됐나**

```js
const sum = rows.reduce((s, r) => s + r.prize.exp * r.count, 0);
const nextGained = Math.min(gained + sum, MAX_EXP);
```

`nextGained`는 `MAX_EXP`(8,999,999,999,999,999 — 2^53 안쪽)로 잘리므로 **저장값은 안전합니다.**

문제는 `sum` 자체와, 반환되는 `rawTotal`, 그리고 요약 패널의 등급별 `p.exp * count`입니다. 초신성 1회가 999,999,999,999,999이므로 **10회만 나와도 1e16으로 2^53을 넘어** 정밀도가 깨집니다. 티켓 999만 장을 "모두 쓰기" 하면 초신성이 10회 안팎 나오므로 실제로 도달합니다.

증상: 요약 패널의 숫자가 미세하게 틀리거나 끝자리가 0으로 뭉갭니다. **저장·레벨 계산은 멀쩡합니다.**

**어떻게 고쳤나** (2026-08-17)

`awardMany`가 `exp * count`를 누적하면서 매번 `Number.isSafeInteger`로 확인하고, 안전 범위를 벗어나는 순간 합산을 멈춰 `exact: false`로 표시합니다. 그 경우 합계는 `MAX_EXP`로 확정합니다.

화면에서는 `exact: false`일 때 **틀린 수를 보여주지 않습니다.**

- 요약 패널 총합 → `상한 도달` / `MAXED`
- 등급별 줄 → 횟수만 남기고 `+EXP` 숨김
- 토스트 → `N회 — 상한 도달`

저장·레벨 계산은 원래부터 `MAX_EXP`에서 잘려 정상이었고, 지금도 그대로입니다.

---

## P2-1. 사다리 타이머 배열이 계속 쌓인다 — ✅ 수정됨

**파일** `frontend/src/components/LadderGame.jsx`

`start()`가 `timersRef.current.push(id)`만 하고 비우지 않아 한 판마다 하나씩 누적됐습니다.

파칭코와 같은 `clearTimers()`(배열을 비우고 `cancelAnimationFrame`까지)를 사다리에도 만들어 `start()` 첫 줄과 언마운트에서 부릅니다.

---

## P2-2. 홈 화면의 파칭코 EXP가 스냅샷 — ✅ 수정됨

**파일** `frontend/src/pages/HomePage.jsx:102`

```js
const [pachinkoExp] = useState(() => getPachinkoExp());
```

스토어를 구독하지 않고 마운트 시점 값을 한 번만 읽었습니다. 라우팅으로 홈에 돌아오면 재마운트되어 갱신되므로 당시 UX에서는 드러나지 않았지만, 홈이 언마운트되지 않는 구조로 바뀌면 즉시 스테일 버그가 됩니다.

**어떻게 고쳤나** (2026-08-17) — `usePachinkoStore(s => s.gained)` 구독으로 교체. `getPachinkoExp` import도 제거했습니다 (`LaunchEvent.jsx`는 계속 쓰므로 함수 자체는 남겨둠).

---

## P2-3. 만렙 근처에서 획득 배너의 "이전 레벨"이 틀린다 — ✅ 수정됨

**파일** `frontend/src/components/PachinkoSystem.jsx:384`, `frontend/src/components/ExpGainBanner.jsx:22`

```jsx
<ExpGainBanner baseExp={baseExp + gained} gainedExp={multi ? multi.totalExp : result.exp} />
```

배너는 `before = baseExp - gainedExp`로 이전 레벨을 역산합니다. 다회 뽑기의 `multi.totalExp`는 **상한에 잘린 실제 반영량**이라 맞지만, 1회 뽑기의 `result.exp`는 **자르기 전 원본**입니다. `MAX_EXP` 상한에 걸린 상태에서 1회 뽑으면 실제 증가분보다 큰 값을 빼서 이전 레벨이 실제보다 낮게 표시됩니다.

초월 만렙에서만 발생합니다.

**어떻게 고쳤나** (2026-08-17) — `award()` / `awardMany()`가 **상한에 잘린 뒤의 실제 증가분**을 반환하도록 하고, 그 값을 `result.actualExp`로 들고 있다가 배너에 넘깁니다. 파칭코와 사다리 양쪽 모두 적용했습니다 (사다리도 같은 버그가 있었습니다 — 원래 보고서에는 파칭코만 적혀 있었습니다).

---

## P3. localStorage 쓰기 실패가 처리되지 않음 — ✅ 수정됨

**파일** `frontend/src/data/pachinkoData.js` (유틸), `pachinkoStore.js`, `plateStore.js`

사파리 프라이빗 모드나 용량 초과 시 `localStorage.setItem`이 throw합니다. 판 정산 한가운데에서 터지면 그 뒤 코드(EXP 반영·상태 갱신)가 통째로 중단됩니다.

**어떻게 고쳤나** (2026-08-17) — `pachinkoData.js`에 `readLS` / `saveLS` / `removeLS` 유틸을 두고 두 스토어의 직접 호출을 전부 교체했습니다. 쿠키를 막아둔 브라우저에서 `getItem`조차 throw하는 경우도 같이 막힙니다. 저장이 실패해도 메모리 상태는 갱신되므로 그 세션 동안은 정상 동작합니다.

**남은 범위** — 이 유틸은 파칭코·원판 계열에만 적용했습니다. `HomePage`의 검색 기록 등 앱의 다른 `localStorage` 직접 호출은 그대로입니다.

---

## 남은 작업

보고서에 올린 항목은 전부 처리했습니다. **운영 전 확인 항목**(아래 표)만 그대로 남아 있습니다.

## P0-1 재현 테스트 결과 (2026-08-17)

헤드리스 Chrome을 CDP로 붙여 실제로 돌렸습니다. 개발 서버(`localhost:5173`) 기준.

**① 연출 도중 새로고침 — 환불되는가**

```
출발 전        steelbody_pachinko_used = null
연출 중(600ms) steelbody_pachinko_used = "100"   (연출 진행중 = true)
새로고침 후    steelbody_pachinko_used = "100"
→ 소모된 티켓 = 100  ✅
```

연출이 아직 돌고 있는 600ms 시점에 이미 `used`가 기록돼 있습니다. 이게 수정의 핵심입니다 — 고치기 전에는 3.8초가 지나야 기록됐으므로 이 시점의 값은 `null`이었고, 새로고침하면 그대로 `null`(= 전액 환불)이었습니다.

**② 연출 도중 이탈 — 보상을 못 받는가** (티켓을 먼저 깎았으니 생길 수 있는 반대 버그)

```
연출 중(600ms) log = 0건        ← 아직 보상 전
SPA 라우팅으로 /home 이동
이탈 후        used = 100, log = 1건 (l_mid, +50000 EXP)
→ 티켓도 나가고 보상도 정산됨  ✅
```

언마운트 정산이 실제로 동작합니다. 티켓만 날리는 새 버그는 생기지 않았습니다.

**부수 확인** — 출발 직후 "모두 쓰기" 버튼이 `🎫 9,999,899`(= 9,999,999 − 100)로 즉시 줄어드는 것도 확인했습니다. 화면상으로도 차감이 시작 시점에 반영됩니다.

테스트 스크립트는 임시 폴더에 있습니다(레포에는 넣지 않았습니다). CDP로 Chrome을 직접 조작하는 방식이라 개발 서버가 떠 있어야 돕니다.

---

## 그전에 이미 고쳤던 것 (2026-08-14 이전, 재발 확인용)

| | 내용 |
|---|---|
| 무한 티켓 | `maxStack`이 표시용 `min()`이라 티켓을 써도 안 줄던 것 → `trimOverflow` |
| `?reset=1` 무효 | 스토어가 import 시점에 localStorage를 먼저 읽어 초기화가 안 먹던 것 |
| `?level=` 무효 | 위와 같은 원인 |
| 계정 전환 시 원판 지갑 잔류 | `authStore`에 `plateStore.reset()` 누락 |
| 자정 넘김 시 미니게임 잠김 | `rollDay`가 `startRun` 안에서만 불려, 판을 다 쓴 날 이후 영구 잠김 |
| 게임 중 이탈 시 판 손실 | 판 수는 깎이고 보상은 0 → 언마운트 정산 추가 |
| 부활 후 조작 불가 | 멈춘 사이 `keyup`을 못 받아 키가 눌린 채 굳던 것 |
| 잭팟 원판 증발 | 바닥 정리가 `shift()`라 900짜리가 밀려나던 것 → 최저값 우선 제거 |
| 모바일 스크롤 오조작 | `window` `pointermove`가 게임판 밖 터치까지 받던 것 |
| 게임 중 교환창 열림 | 게임이 뒤에서 계속 돌아 가려진 채 죽던 것 |
| 검은 화면 | `useEffect` 의존성 배열이 `draw` 선언보다 위에 있어 TDZ 크래시 |

## 개발 전용 설정 (운영 전 확인)

`import.meta.env.DEV`로 묶여 있어 프로덕션 빌드에서는 자동으로 사라집니다. **다만 아래는 예외입니다.**

| 항목 | 개발 | 운영 |
|---|---|---|
| `TICKET_RULE.bonus` / `maxStack` | 9,999,999 | 0 / 150 |
| `PLATE_RULE.dailyPlays` | 9,999 | 5 |
| 파칭코 꽝 가중치 | 30,000 | 530,000 |
| 사다리 꽝 가중치 | 20,000,000 | 800,000,000 |
| **슈퍼울트라 원판 `weight: 0.5`** | **0.5** | **0.5 ← DEV 미적용** |
| **`PLATE_RULE.revives: 3`** | **3** | **3 ← DEV 미적용** |

굵게 표시한 둘은 **운영에 그대로 나갑니다.** 의도한 것인지 확인이 필요합니다.

- 슈퍼울트라 0.5%는 주울 때 기대값을 1.74 → 6.21로 **3.6배** 올립니다 (원판 → 티켓 → 파칭코 EXP로 전파)
- 부활 3회는 사실상 목숨 4개라 한 판이 길어지고, 판당 수급이 함께 늘어납니다

또한 `LADDER.cost: 100`은 원래 **파칭코와 티켓당 기대 EXP를 1.000배로 맞춘 값**입니다. 확률을 건드리면 이 정합이 깨지므로, 운영 확률을 조정할 때는 `cost`를 함께 재계산해야 합니다.
